// services/embedding.service.js
// Stubbed out because we’re running Gemini-only mode.
// This file remains for compatibility.

async function embedTexts() {
  // No embeddings generated in Gemini-only mode
  return []
}

module.exports = { embedTexts }
